export const COLORS = {
  // Backgrounds — soft, calm dark
  bg: "#15161c",
  surface: "#1c1e26",
  raised: "#24262f",
  panel: "#2a2c38",
  border: "#34363f", // matches --line

  // Text
  text: "#f5f5f7",    // matches --t1
  textMuted: "#a8a9b8", // matches --t2
  textDim: "#6b6d7d",   // matches --t3

  // Accents — warm coral/amber + lavender
  accent: "#ff9466",     // matches --acc (coral)
  accentSoft: "#ffb088", // matches --acc-soft
  accentBg: "rgba(255, 148, 102, 0.10)",
  accentLine: "rgba(255, 148, 102, 0.22)",

  lavender: "#b3a1ff",   // matches --lav
  lavenderBg: "rgba(179, 161, 255, 0.10)",

  // Semantic
  green: "#6ee7a8",
  blue: "#7fb6ff",
  purple: "#c9a8ff",
  yellow: "#ffd479",
  red: "#ff8a9b",
  teal: "#6ee0d4",
  muted: "#a8a9b8", // backward compatibility alias
  error: "#ff8a9b", // backward compatibility alias
};

export const TYPE_COLORS = {
  work: "#7fb6ff",       // matches --block-work
  study: "#c9a8ff",      // matches --block-study
  personal: "#6ee7a8",   // matches --block-personal
  health: "#ff9466",     // matches --block-health (coral)
  rest: "#ffd479",       // matches --block-rest
  ent: "#ff8a9b",        // matches --block-ent
};

